# ============================================
# 站点身份
# ============================================
SITE_SLUG=example-site
SITE_DOMAIN=example.com
CMS_SUBDOMAIN=cms
SITE_ENV=local   # local | staging | production

# ============================================
# 前端
# ============================================
WEB_PORT=3000
PUBLIC_SITE_URL=http://localhost:3000
PUBLIC_STRAPI_URL=http://localhost:1337
PUBLIC_IMAGOR_URL=http://localhost:8000
INTERNAL_STRAPI_URL=http://cms:1337
INTERNAL_IMAGOR_URL=http://imagor:8000

# ============================================
# Strapi
# ============================================
CMS_PORT=1337
STRAPI_APP_KEYS=changeme-key1,changeme-key2
STRAPI_API_TOKEN_SALT=changeme
STRAPI_ADMIN_JWT_SECRET=changeme
STRAPI_JWT_SECRET=changeme
STRAPI_TRANSFER_TOKEN_SALT=changeme
# 只读 API token —— 由 admin 面板生成后填入。SSG 构建时必需。
STRAPI_PUBLIC_TOKEN=

# ============================================
# PostgreSQL
# ============================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=strapi
DB_USER=strapi
DB_PASSWORD=changeme
DB_SSL=false

# ============================================
# Imagor
# ============================================
IMAGOR_PORT=8000
IMAGOR_SECRET=changeme
IMAGOR_UNSAFE=1

# ============================================
# Cloudflare Tunnel（替代旧的 CF DNS + certbot 方案）
# ============================================
# 在 Cloudflare Dashboard → Zero Trust → Networks → Tunnels 创建后拿到
# 或用 scripts/cloudflare/setup-tunnel.sh 自动创建
CF_TUNNEL_TOKEN=

# ============================================
# 部署 —— VPS 通过 VPN 访问（无公网 IP）
# ============================================
DEPLOY_HOST=10.0.0.50                   # VPN 内网地址
DEPLOY_USER=deploy
DEPLOY_SSH_KEY=~/.ssh/id_ed25519_vps
DEPLOY_PATH=/srv/example-site

# 本地服务器（staging，局域网）
STAGING_HOST=192.168.1.50
STAGING_USER=deploy
STAGING_SSH_KEY=~/.ssh/id_ed25519_lan
STAGING_PATH=/srv/example-site

# ============================================
# GHCR（SSG 分支只构建 cms 镜像）
# ============================================
GHCR_USER=your-github-username
GHCR_IMAGE_CMS=ghcr.io/your-github-username/example-site-cms
IMAGE_TAG=latest

# ============================================
# Rebuild webhook（SSG 特有）
# ============================================
WEBHOOK_SECRET=changeme-random-string
